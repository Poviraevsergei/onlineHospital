create table users
(
	id bigserial not null
		constraint user_pk
			primary key,
	first_name varchar,
	last_name varchar,
	age integer,
	user_login varchar,
	user_password varchar
);

alter table users owner to postgres;

